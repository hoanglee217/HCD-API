{{#meta}}
output: src/Hcd/Hcd.Api/RequestHandler/{{Module}}/{{Entities}}/Create{{Entity}}Handler.cs
enable: {{useCreateDtos}}
{{/meta}}
using Hcd.Common.Requests.{{Module}}.{{Entities}};
using Hcd.Application.Services.{{Module}};
using MediatR;

namespace Hcd.Api.RequestHandler.{{Module}}.{{Entities}};

public class Create{{Entity}}Handler({{Entity}}Service {{entity}}Service) : IRequestHandler<Create{{Entity}}Request, Create{{Entity}}Response>
{
    public Task<Create{{Entity}}Response> Handle(Create{{Entity}}Request request, CancellationToken cancellationToken)
    {
        return {{entity}}Service.Create{{Entity}}(request);
    }
}